<%= form_with(model: application_key, local: true) do |form| %>
  <% if application_key.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(application_key.errors.count, "error") %> prohibited this application_key from being saved:</h2>

      <ul>
        <% application_key.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
